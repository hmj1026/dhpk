interface:
  display_name: "Harness Setup"
  short_description: "Configure harness assets and setup evidence"
  default_prompt: "Use $harness-setup to configure or inspect harness setup with evidence."
policy:
  allow_implicit_invocation: false
