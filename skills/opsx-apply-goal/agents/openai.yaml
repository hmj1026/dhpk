interface:
  display_name: "Opsx Apply Goal"
  short_description: "Unattended OpenSpec goal generator."
  default_prompt: "Use $opsx-apply-goal to apply the Opsx Apply Goal workflow with its verification rules."
