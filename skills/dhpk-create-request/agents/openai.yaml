interface:
  display_name: "Create Request"
  short_description: "Create, update, or scan per-task request tickets for progress"
  default_prompt: "Use $dhpk-create-request to apply the Create Request workflow with its verification rules."
