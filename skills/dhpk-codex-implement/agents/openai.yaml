interface:
  display_name: "Codex Implement"
  short_description: "Implement features via Codex MCP."
  default_prompt: "Use $dhpk-codex-implement to apply the Codex Implement workflow with its verification rules."
policy:
  allow_implicit_invocation: false
