interface:
  display_name: "Code Explore"
  short_description: "Pure Claude code investigation."
  default_prompt: "Use $dhpk-codebase-exploration to apply the Code Explore workflow with its verification rules."
policy:
  allow_implicit_invocation: false
