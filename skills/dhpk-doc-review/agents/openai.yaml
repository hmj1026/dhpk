interface:
  display_name: "Doc Review"
  short_description: "Document review via Codex MCP."
  default_prompt: "Use $dhpk-doc-review to apply the Doc Review workflow with its verification rules."
policy:
  allow_implicit_invocation: false
