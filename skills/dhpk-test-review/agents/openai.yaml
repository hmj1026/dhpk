interface:
  display_name: "Test Review"
  short_description: "Portable test coverage review with an adequacy gate."
  default_prompt: "Use $dhpk-test-review to apply the Test Review workflow with its verification rules."
policy:
  allow_implicit_invocation: false
