interface:
  display_name: "Project Setup"
  short_description: "Project configuration initialization."
  default_prompt: "Use $dhpk-project-setup to apply the Project Setup workflow with its verification rules."
policy:
  allow_implicit_invocation: false
