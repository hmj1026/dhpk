scope: portable-command-skills
note: Resolve skill entry scripts through the physical Skill directory instead of CLAUDE_PLUGIN_ROOT, report leftover legacy resume helpers during setup, surface runner-log write failures, fix a macOS mktemp collision in post-obs.sh, and state a non-use boundary on every command front door.
