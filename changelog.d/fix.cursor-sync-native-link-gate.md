scope: consumer-gate
note: Cursor project-local consumer validation now requires per-skill native-link Host Bindings and shared `.agents/skills` trees instead of native `managed_entries.skills` copies.
