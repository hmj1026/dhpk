scope: distribution
note: `manifests/profile-projection-sets.json` declares each install profile's Cursor and codex-sync skill projection set; `catalog.js --write` generates it and `--check` fails CI when a set is stale.
