scope: installer
note: Cursor project-local install now materializes skills into `.agents/skills` and binds `.cursor/skills/<name>` as per-skill native-link Host Bindings instead of copying native skill trees.
