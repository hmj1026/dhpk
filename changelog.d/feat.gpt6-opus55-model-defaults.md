scope: model-economics
note: Move Codex role and CLI-worker defaults to GPT-6 (`gpt-6-sol` replaces GPT-5.6 Sol and Terra, `gpt-6-luna` replaces GPT-5.6 Luna), pin Claude Code and Cursor Claude routes to Opus 5.5, and refresh published model pricing; existing `codex_*_model` overrides keep working.
