scope: installer
note: Cursor Host Bindings now choose `direct` only from a Cursor discovery consumer-probe PASS record; missing, failed, or non-discovery evidence keeps per-skill `native-link` fallbacks.
