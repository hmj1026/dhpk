scope: Codex native distribution projection
note: Route Codex native package generation and verification through the shared compiler and staged artifact store while preserving existing CLI behavior and package bytes.
