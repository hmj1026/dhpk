scope: installer
note: Cursor and Codex-sync `--update` now migrates unchanged receipt-owned native skill copies onto shared Host Bindings, keeps hand-edited copies until `--adopt`, and leaves unowned dests in place; `--plan --json` lists each leftover as migrate, keep-modified, or unowned without writing.
