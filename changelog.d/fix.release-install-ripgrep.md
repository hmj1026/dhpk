scope: release-workflow
note: Install ripgrep in the Release workflow before rerunning the bounded repository tests, matching CI, so the isolated code-trace and deploy-list fixtures no longer fail on the Ubuntu runner.
