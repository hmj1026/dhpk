scope: ci
note: Treat a promoted CHANGELOG.md release section as changelog-fragment coverage, so the develop -> main release PR is no longer blocked by the fragment-coverage gate after release preparation consumed every pending fragment.
