scope: installer
note: Codex-sync install now materializes the profile's declared projection set into `.agents/skills` and binds `.codex/skills/<name>` as per-skill Host Bindings. Internal runtime-support skills stay as native copies. A second Host unions the shared tree without rewriting the first Host's bindings.
