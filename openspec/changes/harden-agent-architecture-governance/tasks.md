## 1. Characterization and Pure Projection Contracts (independently shippable)

- [x] 1.1 Lock current Agent Plugin, Codex native, Cursor, and Claude/inventory output trees, ordering, diagnostics, stdout/stderr, exit codes, and symlink behavior in `tests/distribution-projection-characterization.test.js` and `tests/fixtures/distribution-projection-characterization.json` — Action: add behavior-level characterization at the existing CLI/public module seams; Why: establish the zero-breaking oracle before refactoring; Dependencies: none; Risk: M.
- [x] 1.2 Add RED contract tests for `compileDistribution`, `materializeDistribution`, and `verifyDistribution` in `tests/distribution-projection-contract.test.js` — Action: assert deterministic frozen plans, structured results, unplanned-output rejection, closed stage vocabulary, and stage-bound evidence using in-memory boundary doubles; Why: specify the deep interface before implementation; Dependencies: requires 1.1; Risk: L.
- [x] 1.3 Add RED inventory-policy tests in `tests/distribution-inventory-validate.test.js` and `tests/gen-distribution-inventory.test.js` — Action: cover adapter identity, physical owner, transform version, verification stages, policy preservation, default-forbid symlinks, and escaping-link rejection; Why: prove the inventory is the projection SSOT; Dependencies: requires 1.1; Risk: M.
- [x] 1.4 Extend `manifests/distribution-inventory.json` and `scripts/lib/distribution-inventory.js` — Action: add and validate the versioned projection-contract section while preserving hand-authored policy during reconciliation; Why: provide explicit normalized compiler inputs without a second inventory; Dependencies: requires 1.3 RED; Risk: H — failure scenario: reconciliation silently rewrites policy or changes existing surface membership.
- [x] 1.5 Implement frozen DTOs, canonical serialization, fingerprints, and one structured result/error strategy in `scripts/lib/distribution-projection-contract.js` — Action: make expected values independently testable and validate only external inputs; Why: give every stage one stable contract; Dependencies: requires 1.2 RED and 1.4; Risk: M.
- [x] 1.6 Implement the pure facade in `scripts/lib/distribution-compiler.js` — Action: expose only `compileDistribution`, `materializeDistribution`, and `verifyDistribution`, with injected adapters/stores and no ambient filesystem/process reads; Why: create the deep module while leaving production CLIs untouched; Dependencies: requires 1.5; Risk: H — failure scenario: compiler ordering or fingerprint rules accidentally encode a surface-specific assumption.
- [x] 1.7 Verify Phase 1 with `node --test tests/distribution-projection-characterization.test.js tests/distribution-projection-contract.test.js tests/distribution-inventory-validate.test.js tests/gen-distribution-inventory.test.js tests/distribution-scoped-counts.test.js` and `node --test --experimental-test-coverage tests/distribution-projection-contract.test.js` — Action: require GREEN characterization/contracts and at least 80% line/function coverage for the new contract/compiler modules; Why: make the unused-contract slice mergeable; Dependencies: requires 1.1-1.6; Risk: L.

## 2. ProjectionArtifactStore (independently shippable)

- [x] 2.1 Add RED filesystem-port tests in `tests/projection-artifact-store.test.js` — Action: cover planned writes, traversal rejection, physical-ancestor containment, staging failure, atomic publish/abort, default-forbid links, in-root relative links, absolute/escaping links, and post-write digests; Why: specify the security-sensitive mutation seam first; Dependencies: requires Phase 1; Risk: M.
- [x] 2.2 Implement `ProjectionArtifactStore` in `scripts/lib/projection-artifact-store.js` — Action: provide only bounded staged operations accepted from a compiled plan and preserve the previously published root on failure; Why: establish one physical writer and eliminate future per-surface safety drift; Dependencies: requires 2.1 RED; Risk: H — failure scenario: a failed publish removes or partially replaces a previously valid generated package.
- [x] 2.3 Extend `tests/distribution-projection-contract.test.js` and `scripts/lib/distribution-compiler.js` — Action: prove `materializeDistribution` rejects adapter outputs outside the plan and returns an artifact bound to both plan and observed output fingerprints through the real store port; Why: integrate the port without cutting over a production surface; Dependencies: requires 2.2; Risk: M.
- [x] 2.4 Verify Phase 2 with `node --test tests/projection-artifact-store.test.js tests/distribution-projection-contract.test.js tests/distribution-projection-characterization.test.js` and `node --test --experimental-test-coverage tests/projection-artifact-store.test.js` — Action: require GREEN rollback/security cases and at least 80% line/function coverage for the new store; Why: keep the infrastructure slice independently mergeable; Dependencies: requires 2.1-2.3; Risk: L.

## 3. Agent Plugin Projection Cutover (independently shippable)

- [ ] 3.1 Add RED legacy-versus-plan equivalence cases in `tests/agent-plugin-package.test.js`, `tests/gen-agent-plugin-package.test.js`, and `tests/validate-agent-plugin-package.test.js` — Action: compare selected stable IDs, manifests, README/content bytes, transforms, fingerprints, diagnostics, and failure exits; Why: create the Agent Plugin cutover gate at public seams; Dependencies: requires Phase 2; Risk: M.
- [ ] 3.2 Refactor `scripts/lib/agent-plugin-package.js` — Action: retain Agent Plugin rendering/consumer checks as narrow adapter behavior and route filesystem writes through the injected store without changing exported compatibility functions; Why: remove selection and mutation ownership from the surface module; Dependencies: requires 3.1 RED; Risk: H — failure scenario: portable manifest adaptation or credential/path safety changes observably.
- [ ] 3.3 Wire `scripts/ci/gen-agent-plugin-package.js` and `scripts/ci/validate-agent-plugin-package.js` through `scripts/lib/distribution-compiler.js` — Action: compose inventory, adapter, store, and verification stage behind existing CLI arguments/messages/exits; Why: cut over one surface without a public v2 command; Dependencies: requires 3.2; Risk: H — failure scenario: the CLI publishes a partial package or maps a structured error to a different exit contract.
- [ ] 3.4 Extend `tests/distribution-rollback-proof.test.js` and `tests/distribution-projection-characterization.test.js` — Action: prove failed Agent Plugin staging retains the accepted tree and compiler/legacy fixtures remain byte-equivalent; Why: make rollback evidence explicit; Dependencies: requires 3.3; Risk: M.
- [ ] 3.5 Verify Phase 3 with `node --test tests/agent-plugin-package.test.js tests/gen-agent-plugin-package.test.js tests/validate-agent-plugin-package.test.js tests/validate-distribution.test.js tests/distribution-rollback-proof.test.js tests/distribution-projection-characterization.test.js` — Action: require all existing and new Agent Plugin gates GREEN before removing no helper; Why: make the first surface cutover independently shippable; Dependencies: requires 3.1-3.4; Risk: L.

## 4. Codex Native Projection Cutover (independently shippable)

- [ ] 4.1 Add RED equivalence and consumer-stage cases in `tests/codex-native-package-validate.test.js`, `tests/gen-codex-native-package.test.js`, and `tests/verify-codex-native-package.test.js` — Action: lock skill selection, identity/frontmatter checks, generated bytes, structural versus consumer evidence, diagnostics, and exits; Why: define the Codex cutover seam; Dependencies: requires Phase 3; Risk: M.
- [ ] 4.2 Refactor `scripts/lib/codex-native-package.js` — Action: retain Codex-native rendering/probing as adapter behavior and delegate selection, physical writes, and evidence envelopes to the shared contracts; Why: prevent a second projection policy; Dependencies: requires 4.1 RED; Risk: H — failure scenario: native skill identity or install-path containment weakens.
- [ ] 4.3 Wire `scripts/ci/gen-codex-native-package.js` and `scripts/ci/verify-codex-native-package.js` through `scripts/lib/distribution-compiler.js` — Action: preserve existing CLI parameters and output mapping while using compiled plans/artifacts/evidence; Why: cut over Codex under the One-Version Rule; Dependencies: requires 4.2; Risk: H — failure scenario: structural success incorrectly upgrades Codex runtime support.
- [ ] 4.4 Extend `tests/distribution-rollback-proof.test.js` and `tests/distribution-projection-characterization.test.js` — Action: add Codex staging rollback and byte/diagnostic equivalence proof; Why: retain per-surface reversal; Dependencies: requires 4.3; Risk: M.
- [ ] 4.5 Verify Phase 4 with `node --test tests/codex-native-package-validate.test.js tests/gen-codex-native-package.test.js tests/verify-codex-native-package.test.js tests/codex-native-install-smoke.test.js tests/distribution-rollback-proof.test.js tests/distribution-projection-characterization.test.js` — Action: require package, verification, smoke, and rollback gates GREEN; Why: make Codex cutover independently shippable; Dependencies: requires 4.1-4.4; Risk: L.

## 5. Cursor Projection Cutover (independently shippable)

- [ ] 5.1 Add RED equivalence and support-tier cases in `tests/cursor-plugin-package.test.js`, `tests/gen-cursor-plugin-package.test.js`, and `tests/validate-cursor-plugin-package.test.js` — Action: lock shared/overlay selection, native transforms, hooks/rules/agents/commands, generated bytes, consumer states, diagnostics, and exits; Why: define Cursor's wider adapter contract; Dependencies: requires Phase 4; Risk: M.
- [ ] 5.2 Refactor `scripts/lib/cursor-plugin-package.js` — Action: retain Cursor-native rendering and probe logic while removing independent selection, ownership, direct writes, and evidence formatting; Why: make the most complex surface obey the same plan/store boundary; Dependencies: requires 5.1 RED; Risk: H — failure scenario: shared Agent Plugin ownership is duplicated or overlay transforms lose provenance.
- [ ] 5.3 Wire `scripts/ci/gen-cursor-plugin-package.js` and `scripts/ci/validate-cursor-plugin-package.js` through `scripts/lib/distribution-compiler.js` — Action: preserve public CLI behavior and map unavailable consumers to current support states; Why: complete Cursor cutover without overstating runtime support; Dependencies: requires 5.2; Risk: H — failure scenario: `NOT_CONFIGURED` or `UNAVAILABLE` is reported as PASS.
- [ ] 5.4 Extend `tests/distribution-rollback-proof.test.js` and `tests/distribution-projection-characterization.test.js` — Action: prove Cursor rollback plus shared/overlay byte and provenance equivalence; Why: isolate failure to this surface; Dependencies: requires 5.3; Risk: M.
- [ ] 5.5 Verify Phase 5 with `node --test tests/cursor-plugin-package.test.js tests/gen-cursor-plugin-package.test.js tests/validate-cursor-plugin-package.test.js tests/validate-distribution.test.js tests/distribution-rollback-proof.test.js tests/distribution-projection-characterization.test.js` — Action: require Cursor package, support-tier, shared-owner, and rollback gates GREEN; Why: make Cursor cutover independently shippable; Dependencies: requires 5.1-5.4; Risk: L.

## 6. Claude and Inventory Projection Cutover (independently shippable)

- [ ] 6.1 Add RED Claude/inventory plan-equivalence cases in `tests/distribution-inventory-validate.test.js`, `tests/gen-distribution-inventory.test.js`, `tests/validate-distribution.test.js`, and `tests/distribution-scoped-counts.test.js` — Action: lock Claude skill roots, lifecycle/surface membership, description counts, ordering, projection-policy preservation, and diagnostics; Why: guard the inventory's own bootstrap path from self-derived policy; Dependencies: requires Phase 5; Risk: M.
- [ ] 6.2 Refactor `scripts/lib/distribution-inventory.js` and `scripts/ci/gen-distribution-inventory.js` — Action: compile Claude outputs and reconciled inventory views through the shared plan while retaining inventory parsing/normalization at the boundary; Why: remove the last generator-local selection path; Dependencies: requires 6.1 RED; Risk: H — failure scenario: the bootstrap command treats derived directory state as authoritative and overwrites the SSOT.
- [ ] 6.3 Wire `scripts/ci/validate-distribution.js` through `verifyDistribution` — Action: emit plan/artifact-bound structural/package evidence while preserving current reports and exit behavior; Why: use the shared verification contract for the cross-surface gate; Dependencies: requires 6.2; Risk: H — failure scenario: passing one projection masks another surface failure.
- [ ] 6.4 Extend `tests/distribution-projection-characterization.test.js` and `tests/distribution-rollback-proof.test.js` — Action: prove Claude/inventory equivalence, failed reconciliation rollback, and per-surface failure isolation; Why: close the final distribution cutover safely; Dependencies: requires 6.3; Risk: M.
- [ ] 6.5 Verify Phase 6 with `node --test tests/distribution-inventory-validate.test.js tests/gen-distribution-inventory.test.js tests/validate-distribution.test.js tests/distribution-scoped-counts.test.js tests/distribution-projection-characterization.test.js tests/distribution-rollback-proof.test.js` — Action: require all inventory and cross-surface gates GREEN; Why: make the full projection pipeline shippable before orchestration changes; Dependencies: requires 6.1-6.4; Risk: L.

## 7. Orchestration Evidence and Sentinel Ownership (independently shippable)

- [ ] 7.1 Add RED ownership and lifecycle scenarios in `tests/review-lifecycle.test.js`, `tests/resumed-review-reconcile.test.js`, and `tests/parallel-dispatch-contract.test.js` — Action: prove orchestration owns dispatch/handoff identities, retries remain linked, passing messages cannot clear debt, terminal lifecycle plus armed Sentinel is incomplete, and unrelated work gets a new identity; Why: characterize the enforcement boundary before policy or lifecycle edits; Dependencies: requires Phase 1 contracts; Risk: M.
- [ ] 7.2 Extend `scripts/hooks/_lib/review-lifecycle.sh` and `scripts/hooks/_lib/resumed-review-obligation.sh` — Action: add backward-compatible task/attempt, producer, wave, scope, adapter/stage, and optional plan/artifact fingerprint fields while preserving sentinel names, slot lookup, verdict eligibility, and hook-owned clearance; Why: make handoff/evidence identity durable without a second store; Dependencies: requires 7.1 RED; Risk: H — failure scenario: old records become unreadable or foreign evidence qualifies for clearance.
- [ ] 7.3 Update `skills/dhpk-execution-policy/references/implementation-dispatch.md` and `skills/dhpk-execution-policy/references/review-gate-mechanics.md` — Action: state the orchestration-versus-Sentinel ownership split, required handoff identity, and evidence presentation sequence without duplicating the dispatch table or clearance implementation; Why: make coordination responsibility explicit to agents; Dependencies: requires 7.2; Risk: M.
- [ ] 7.4 Update `rules/execution-policy.md` — Action: keep implementation dispatch as the policy SSOT and add only the contract pointer/acceptance rule that terminal orchestration plus resolved Sentinel gates is required; Why: bind downstream flows while avoiding policy forks; Dependencies: requires 7.3; Risk: M.
- [ ] 7.5 Extend `tests/stop-review-reconcile.test.js`, `tests/stop-dispatch-audit.test.js`, and `tests/reviewer-contract.test.js` — Action: prove current artifacts clear exactly as before, missing/foreign identifiers fail closed, and no new component directly clears Sentinel debt; Why: protect the enforcement core during additive identity migration; Dependencies: requires 7.2-7.4; Risk: H — failure scenario: a valid existing review can no longer clear or an invalid new result can clear.
- [ ] 7.6 Verify Phase 7 with `node --test tests/review-lifecycle.test.js tests/resumed-review-reconcile.test.js tests/parallel-dispatch-contract.test.js tests/stop-review-reconcile.test.js tests/stop-dispatch-audit.test.js tests/reviewer-contract.test.js tests/sentinel-clear-core.test.js tests/sentinel-slots.test.js` — Action: require lifecycle, handoff, enforcement, and slot compatibility GREEN; Why: make orchestration hardening shippable without any distribution release dependency; Dependencies: requires 7.1-7.5; Risk: L.

## 8. Compatibility Residue, Documentation, and Release Gates (independently shippable)

- [ ] 8.1 Remove only proven-unreachable duplicated selection/materialization helpers from `scripts/lib/agent-plugin-package.js`, `scripts/lib/codex-native-package.js`, `scripts/lib/cursor-plugin-package.js`, and `scripts/lib/distribution-inventory.js` — Action: use `cx references` and the characterization suite before each deletion; Why: retire compatibility residue without broad cleanup; Dependencies: requires Phases 3-6; Risk: H — failure scenario: an undocumented caller still depends on a removed helper.
- [ ] 8.2 Update `docs/distribution-surfaces.md`, `docs/distribution-surfaces.zh-TW.md`, and `docs/contracts/review-lifecycle.md`, and create `docs/adr/0009-distribution-projection-and-orchestration-ownership.md` — Action: document the compiler/store/adapters, inventory and symlink SSOT, support-stage limits, orchestration/Sentinel split, migration/rollback, and ADR consequences in English and Traditional Chinese where mirrored; Why: make installation/operator-facing behavior and architecture discoverable; Dependencies: requires 8.1 and Phase 7; Risk: M.
- [ ] 8.3 Run `npx gitnexus analyze` if the index is stale, then run the repository's GitNexus changed-symbol detection against `scripts/lib/distribution-compiler.js`, `scripts/lib/projection-artifact-store.js`, the migrated surface modules, and lifecycle helpers — Action: confirm no unexpected callers/processes or reverse dependencies remain; Why: satisfy the repo's indexed-symbol gate before review/commit; Dependencies: requires 8.1-8.2; Risk: M.
- [ ] 8.4 Run `node --test tests/distribution-projection-characterization.test.js tests/distribution-projection-contract.test.js tests/projection-artifact-store.test.js tests/distribution-inventory-validate.test.js tests/validate-distribution.test.js tests/agent-plugin-package.test.js tests/codex-native-package-validate.test.js tests/cursor-plugin-package.test.js tests/review-lifecycle.test.js tests/resumed-review-reconcile.test.js tests/sentinel-clear-core.test.js` — Action: require the focused architecture and zero-breaking suite GREEN; Why: provide final scoped acceptance evidence; Dependencies: requires 8.1-8.3; Risk: L.
- [ ] 8.5 Run `bash scripts/validate/validate-harness.sh` and `npx openspec validate harden-agent-architecture-governance --strict` — Action: record PASS/FAIL, preserve any proven pre-existing warnings separately, and do not claim runtime support from structural-only results; Why: satisfy full harness/spec exit gates; Dependencies: requires 8.4; Risk: M.
- [ ] 8.6 Run the applicable code, security, frontend, database/migration, and documentation reviewer gates derived from the final edited-file list and reconcile every current Sentinel through the existing hook-owned path — Action: require fresh scope-bound artifacts and no pending obligation before delivery; Why: prove the architecture migration did not bypass governance; Dependencies: requires 8.5; Risk: H — failure scenario: a terminal implementation is reported complete while a review gate remains unresolved.

## Success Criteria

- [ ] 9.1 Every phase is mergeable and reversible on its own, all characterized CLI/artifact observables remain identical, new contract/store modules meet the 80% line/function coverage bar, all applicable focused/full gates pass, support tiers remain stage-honest, and no current Sentinel obligation remains unresolved.
