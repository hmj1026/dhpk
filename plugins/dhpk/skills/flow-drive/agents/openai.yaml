interface:
  display_name: "Flow Drive"
  short_description: "Route or implement confirmed work"
  default_prompt: "Use $flow-drive to route this task or implement its confirmed specification with bounded verification."
policy:
  allow_implicit_invocation: false
